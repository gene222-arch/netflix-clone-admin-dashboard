import { makeStyles } from '@material-ui/core';

const inputFieldUseStyles = makeStyles(theme => ({
    cardContent: {
        padding: '3rem'
    },
    checkBoxContainer: {
        width: '100%',
        margin: '0.5rem 0'
    },
    progress: {
        margin: '.69rem .69rem'
    }
}));

export default inputFieldUseStyles